export enum AppRunningEnvEnum {
  HTTP = 'HTTP',
  NATS = 'NATS',
}

export enum NodeEnvEnum {
  DEVELOPMENT = 'development',
  STAGING = 'staging',
  PROVISION = 'provision',
  PRODUCTION = 'production',
}
